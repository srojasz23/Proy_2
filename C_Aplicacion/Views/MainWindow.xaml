<Window x:Class="C_Aplicacion.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C_Aplicacion.Views"
        mc:Ignorable="d"
        
        Title="Sistema Académico — WPF" Height="520" Width="820"
        Background="{StaticResource SurfaceBrush}">
        

    <Window.DataContext>
        <vm:MainViewModels />
    </Window.DataContext>

    <Grid Margin="0,0,-13,-6">
        <Grid.RowDefinitions>
            <RowDefinition Height="96" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        
        <Grid Grid.Row="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                    <GradientStop Color="{StaticResource PrimaryDarkColor}" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock Text="Sistema Académico — WPF" Foreground="White"
                       FontSize="20" FontWeight="Bold" Margin="16,28,0,0"/>
        </Grid>

      
        <ScrollViewer Grid.Row="1" Padding="16">
            <StackPanel Orientation="Vertical">

                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Registro rápido de alumnos" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBox Width="180" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" ToolTip="Ingrese nombre"/>
                            <TextBox Width="180" Text="{Binding Apellido, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" ToolTip="Ingrese apellido"/>
                            <Button Style="{StaticResource PrimaryButton}" Content="Agregar" Margin="8,0,0,0" Click="Agregar_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Listado de alumnos" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <DataGrid ItemsSource="{Binding Alumnos}" AutoGenerateColumns="False" CanUserAddRows="False"
                                  HeadersVisibility="Column" MinHeight="260">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}" Width="*"/>
                                <DataGridTextColumn Header="Sección" Binding="{Binding Seccion}" Width="100"/>
                                <DataGridTextColumn Header="Ciclo" Binding="{Binding Ciclo}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>


    
                